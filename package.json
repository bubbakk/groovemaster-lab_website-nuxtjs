{
  "name": "groovemaster-lab-website-nuxtjs",
  "dependencies": {
    "bootstrap-vue": "^2.21.2",
    "nuxt": "^2.15.8",
    "nuxt-i18n": "^6.28.1"
  },
  "scripts": {
    "dev": "nuxt",
    "generate": "nuxt generate",
    "dev:host": "nuxt --hostname '0' --port 8000",
    "build": "nuxt build",
    "start": "nuxt start",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore ."
  }
}
